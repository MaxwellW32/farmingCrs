# FastAPI Starter

## Setup

1. Install dependencies:

```bash
python -m pip install -r requirements.txt
```

2. Run the app:

```bash
uvicorn app.main:app --reload
```

## Default endpoints

- `GET /` - Liveness check
